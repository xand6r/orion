TELEGRAM_BOT_TOKEN=
ALLOWED_CHAT_IDS=
HELIUS_API_KEY=
# Optional: free Standard tier (30k CU/month) works but is tight; creator-holding
# and holder-count checks are skipped entirely without it.
BIRDEYE_API_KEY=
# Optional: path to app config JSON (defaults to ./config/app.json)
# APP_CONFIG_PATH=./config/app.json
